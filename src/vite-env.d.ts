/// <reference types="vite/client" />

// Injected by Vite's `define` (see vite.config.ts) — the version from
// package.json, inlined at build time as a string literal.
declare const __APP_VERSION__: string;
